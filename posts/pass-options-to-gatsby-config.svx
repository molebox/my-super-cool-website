---
title: Pass options to gatsby-config.js
category: Gatsby
description: How to pass options to a gatsby config file. Useful for plugins and themes
published: true
date: 2019
---

To pass options to the gatsby-config.js

```js
module.exports = (options) => {
    const {
        lots,
        and,
        lots,
        and,
        lots,
        of,
        options
    } = options;

   return {
    plugins: [...myplugins]
  };
```